import assert from 'node:assert/strict';import master from '../src/web/domain/market/market_briefing_master.js';import normalizer from '../src/web/domain/market/market_briefing_provider_normalizer.js';import snapshots from '../src/web/domain/market/market_briefing_snapshot_store.js';import gate from '../src/web/domain/market/market_briefing_operational_gate.js';import store from '../src/web/domain/market/market_briefing_runtime_store.js';
const mem=new Map();globalThis.localStorage={getItem:k=>mem.get(k)||null,setItem:(k,v)=>mem.set(k,v)};globalThis.MarketBriefingMaster=master;globalThis.MarketBriefingProviderNormalizer=normalizer;globalThis.MarketBriefingSnapshotStore=snapshots;globalThis.MarketBriefingOperationalGate=gate;globalThis.MarketBriefingRuntimeStore=store;const runtime=(await import('../src/web/domain/market/market_briefing_runtime.js')).default||globalThis.MarketBriefingRuntime;
const d='2026-09-18',receivedAt=`${d}T06:30:00+09:00`;runtime.ingestBenchmarks({KOSPI:{value:1},KOSDAQ:{value:1},KOSPI200:{value:1},SP500:{value:1},NASDAQ100:{value:1,delayed:true},SOX:{value:1},VIX:{value:1},USDKRW:{value:1}},{tradingDate:d,receivedAt,status:'FINAL'});let s=store.load(localStorage);s.observations=master.upsertObservation(s.observations,{seriesId:'K200_NIGHT',tradingDate:d,value:1,market:'KRX',session:'NIGHT',source:'KIS',status:'FINAL',observedAt:`${d}T06:00:00+09:00`,receivedAt:`${d}T06:00:01+09:00`});store.save(localStorage,s);assert.equal(runtime.readiness(d,'MORNING').publishable,true);const out=runtime.release(d,'MORNING',gate.REQUIRED_BY_CHECKPOINT.MORNING,{scenario:{base:'hold'}});assert.equal(out.snapshot.scenario.base,'hold');assert.equal(runtime.continuity(d).morning.scenario.base,'hold');console.log('브리핑 브라우저 런타임 회귀검사 통과');